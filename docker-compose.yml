services:
  php:
    # Per-version tag. Without it every PHP_VERSION would share one image name
    # and a run without --build would silently reuse whichever version was
    # built last — green results from the wrong interpreter.
    image: os-query-digest:php${PHP_VERSION:-8.3}
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        PHP_VERSION: ${PHP_VERSION:-8.3}
    working_dir: /app
    # Run as the host user, otherwise everything written into the bind mount
    # lands owned by root and the host can no longer update it.
    user: "${DOCKER_UID:-1000}:${DOCKER_GID:-1000}"
    volumes:
      - .:/app
    environment:
      COMPOSER_HOME: /tmp/composer
      # One vendor dir per PHP version: PHPUnit differs between 7.4 and 8.5, and
      # a shared one would make every run downgrade the previous version's
      # packages. It lives in the bind mount so it inherits host ownership —
      # a named volume would be created root-owned and unwritable here.
      COMPOSER_VENDOR_DIR: /app/.vendor/php${PHP_VERSION:-8.3}
    # composer.lock is shared through the bind mount, so it is dropped before
    # resolving and after: each PHP gets its own highest-compatible PHPUnit and
    # leaves no version-specific lock behind. A library commits no lock.
    command: >
      sh -c "php -v | head -1 &&
             rm -f composer.lock &&
             composer update --no-progress &&
             $$COMPOSER_VENDOR_DIR/bin/phpunit --bootstrap $$COMPOSER_VENDOR_DIR/autoload.php;
             status=$$?;
             rm -f composer.lock;
             exit $$status"

  # Mutation testing. Its own service rather than a flag on `php`, because it
  # needs two things the unit suite deliberately does not have: a coverage
  # driver, and PHP 8.3 — Infection's floor, while this library's is 7.4.
  mutation:
    image: os-query-digest:mutation
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        # Pinned to the version tools/infection/composer.lock was resolved for.
        # Infection's own floor is 8.3, but its dependencies have since moved
        # past it, and a lock is only reproducible on a PHP that satisfies it.
        PHP_VERSION: "8.5"
        WITH_PCOV: "1"
    working_dir: /app
    user: "${DOCKER_UID:-1000}:${DOCKER_GID:-1000}"
    volumes:
      - .:/app
    environment:
      COMPOSER_HOME: /tmp/composer
      # No COMPOSER_VENDOR_DIR override, unlike `php`: Infection invokes PHPUnit
      # through phpunit.xml.dist, which bootstraps vendor/autoload.php, so
      # sending composer elsewhere leaves it with no autoloader.
      #
      # Narrow a run without editing this file:
      #   INFECTION_ARGS="--filter=src/Parser" make mutation
      INFECTION_ARGS: ${INFECTION_ARGS:-}
    command: >
      sh -c "rm -f composer.lock &&
             composer update --no-progress &&
             COMPOSER_VENDOR_DIR=/app/tools/infection/vendor composer --working-dir=tools/infection install --no-progress &&
             tools/infection/vendor/bin/infection --threads=max --show-mutations --no-progress --no-interaction $$INFECTION_ARGS;
             status=$$?;
             rm -f composer.lock;
             exit $$status"


  # Real clusters, for `make certify`. Behind a profile so they never start for
  # the unit suite, which is offline and must stay that way.
  #
  # Security is off on purpose: these nodes are thrown away, and 2.12+ otherwise
  # refuses to boot without OPENSEARCH_INITIAL_ADMIN_PASSWORD.
  os2:
    profiles: [certify]
    image: opensearchproject/opensearch:${OS2_VERSION:-2}
    environment:
      discovery.type: single-node
      DISABLE_SECURITY_PLUGIN: "true"
      # The default heap is a quarter of host RAM, which on a dev machine is
      # gigabytes for an index that stays empty.
      OPENSEARCH_JAVA_OPTS: -Xms512m -Xmx512m
      # A full-ish disk trips the flood-stage watermark and index creation 403s
      # with index_create_block_exception, unrelated to the queries under test.
      cluster.routing.allocation.disk.threshold_enabled: "false"
    ports:
      - "9202:9200"
    healthcheck:
      test: ["CMD-SHELL", "curl -fs http://localhost:9200/_cluster/health || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 40

  os3:
    profiles: [certify]
    image: opensearchproject/opensearch:${OS3_VERSION:-3}
    environment:
      discovery.type: single-node
      DISABLE_SECURITY_PLUGIN: "true"
      OPENSEARCH_JAVA_OPTS: -Xms512m -Xmx512m
      # As os2, and it bites harder: 3.x adds a headroom to the flood-stage
      # watermark, so the block trips under ~100 GB free whatever the
      # percentage. Index creation then 403s with index_create_block_exception,
      # which mentions no disk, and every integration test fails in setUp.
      cluster.routing.allocation.disk.threshold_enabled: "false"
    ports:
      - "9203:9200"
    healthcheck:
      test: ["CMD-SHELL", "curl -fs http://localhost:9200/_cluster/health || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 40
