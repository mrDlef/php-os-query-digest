{
    "_comment": "Infection lives in its own manifest on purpose. Every tool in the root require-dev installs on PHP 7.4, which is the floor this library supports; Infection needs 8.3. Putting it in the root would either break `composer update` on 7.4 or drag in a 2021-era release nobody runs. It does not share the project's dependency tree, so it has no business in it.",
    "require": {
        "infection/infection": "^0.35"
    },
    "config": {
        "allow-plugins": {
            "infection/extension-installer": true
        }
    }
}
